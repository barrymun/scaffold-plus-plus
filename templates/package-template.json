{
  "scripts": {
    "prepare": "husky",
    "lint": "eslint .",
    "lint-fix": "eslint . --fix",
    "test": "vitest run",
    "cspell": "cspell --config cspell.json \"**/*.{ts,tsx,js,jsx,html,json}\" --quiet"
  }
}
